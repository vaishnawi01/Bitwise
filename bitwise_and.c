#include <stdio.h>
#include <stdlib.h>

/*bitwise operators*/
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() 
{
int a,b;
scanf("%d", &a);
scanf("%d", &b);

printf("the output of bitwise operator AND is:%d", a&b);
	return 0;
}
